Requests ohne Clustering
    --> mehrere Apps gespawned, auf jeden Core?
Requests mit Clustering
    --> nur ein Worker
    --> Worker Anzahl = CPU Anzahl
    --> Worker Anzahl > CPU Anzahl


RoundRobin
comp
